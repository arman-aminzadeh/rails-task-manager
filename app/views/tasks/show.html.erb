<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<div>
    <%= link_to "Edit your task", edit_task_path(@task.id) %>
</div>

<div>
    <%= link_to "Delete this task", delete_task_path(@task.id), method: :delete, data: {confirm:'Are you sure?' } %>
</div>

<div>
    <%= link_to "back to list", tasks_path %>
</div>
